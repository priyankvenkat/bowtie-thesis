You are given technical free-text descriptions from an FMEA document about {part_name}.  
Convert this information into structured Bowtie diagram JSON.

Each object must contain:
- "critical_event": the main failure being described
- "causes": one or more technical causes
- "mechanism": use "Mechanism" if none available
- "preventive_barrier": use "Barrier" if none available
- "consequences": the outcomes or effects

If multiple failure scenarios are mentioned, return multiple JSON objects in a list.

Return only valid JSON. Do not include any explanation or extra text.
